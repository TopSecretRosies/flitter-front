import { createStore } from 'vuex'
import auth from '../modules/auth/state'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    auth
  }
})
